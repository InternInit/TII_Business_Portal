## Pass back the result from DynamoDB. **
#set( $modMap = $ctx.result )
#set( $millDiff = $util.time.parseISO8601ToEpochMilliSeconds($modMap.dueDate) - $util.time.nowEpochMilliSeconds())
#set( $millDiv = (1000*60*60*24))
#set( $daysUntil = $millDiff / $millDiff)
#set( $dummy = $modMap.put("daysUntil", $daysUntil))
$util.toJson($ctx.result)